# 클코 구조도 — 부속 자료 보관함

> **정본(SSOT)은 상위 폴더의 [`docs/claude-stack-map.html`](../claude-stack-map.html) 단일 파일**이다.
> 관리 방식은 `docs/master-plan.html` 관례와 동일 (PeterJ 확정 2026-07-11):
> 파일 편집 → 문서 안 "버전 기록" 맨 위에 새 줄(옛 줄 삭제 금지) → 같은 아티팩트 URL 재배포 → 커밋.
> 과거 전체 본문은 git 히스토리가 보존한다 (`git log --follow docs/claude-stack-map.html`).

이 디렉터리는 git 히스토리로 복원할 수 없는 부속 자료만 둔다:

- `2026-07-v1-one-page-summary.jpg` — v1(한 장 요약판) 아티팩트 스크린샷.
  git 도입 전 아티팩트로만 존재했던 판이라 이미지로 보존.

연혁 메모: 2026-07-11 최초 규약은 "날짜별 HTML 스냅샷 누적"이었으나, 같은 날
마스터플랜 방식(단일 정본 + git)으로 전환·통일함.
