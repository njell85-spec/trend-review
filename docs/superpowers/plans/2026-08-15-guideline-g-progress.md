# 가이드라인 개편 G0~G10 — 진행 장부 (세이브 포인트)

> **이 파일의 용도**: 세션이 한도에 걸려 끊겨도 다음 세션이 **이 파일 하나로 이어받는다.**
> 설계 정본은 `2026-08-14-guideline-selection-redesign-plan.md`(G0~G10), 계약 기준선은
> `REPORT_SPEC.md` §1-C. 이 파일은 **무엇이 끝났고 다음이 무엇인지**만 적는다.
>
> 갱신 규칙: 한 덩어리를 커밋할 때마다 그 줄을 갱신하고 **같은 커밋에 넣는다.**
> 그래야 `git log` 와 이 표가 어긋나지 않는다.

## 이 세션의 위임 원칙 (PeterJ 지시 2026-08-15)

- **중요 계획·분석** = Fable 또는 코덱스 F vs Sol 토론
- **중간 취합·검수·커밋** = 본 세션(Opus)
- **하청 구현** = codex Sol / Luna, **CLI 직접 호출 + 출력은 파일로**(`rulebook/dispatch.md` §3-1)
- **최종 검수는 본 세션이 직접 명령을 다시 돌려 원문 수치를 본다.** 하청의 "통과했다"를 옮기지 않는다.
- 변이(mutation) 없이 초록만으로 완료를 주장하지 않는다.

## 이 컨테이너의 제약 (실측 2026-08-15)

- **아웃바운드 네트워크 전면 차단** — PubMed·학회 사이트 전부 `http=000`.
  → ① G5 의 실물 selector 는 **검증 불가**. 어댑터 골격과 fixture 만 만들고
    `config/guideline-orgs.json` 의 `sources` 는 **비워 둔다.** 채우면 거짓이 된다.
    ② 실물 재생·백필 실험은 **GitHub Actions 에서** 돌린다(`workflow_dispatch`).

## 진행표

| | 덩어리 | 상태 | 커밋 | 비고 |
|---|---|---|---|---|
| G0 | 계약·회귀 보호 | ✅ | `563c6e9` | 테스트 23건 · 변이 6종 적색 |
| G1 | 기관 스키마 + 전용 스코어러 | ✅ | `86044e4` | 런타임 미배선 · 변이 4종 적색 |
| — | (곁가지) arm F 스크리닝 상한 1000→5000 | ✅ | `556a377` 외 | 45% 미채점 구멍 |
| G2 | 문서 성격 판정 + 오탐 corpus | ✅ | (아래) | corpus 34건 · 소급 판정으로 결함 2건 회수 |
| G3 | 상태 v2 + 무손실 마이그레이션 | ✅ | (아래) | 현행 7건 무손실 · 손상 파일 위장 금지 |
| G4 | PubMed 수집 확대 | ✅ | (아래) | 초집합 검증 + 쿼리별 실행 증거 |
| G5 | 학회 어댑터 골격 + health | ✅ | (아래) | sources 0건 유지를 테스트로 못 박음 |
| G6 | 계보·supersede | ✅ | (아래) | 애매하면 자동 처리 금지 · 발행 기록 삭제 0 |
| G7 | 오케스트레이터 배선 | ⬜ | | 의존 G4·G5·G6 — **G4/G5 와 같은 커밋 금지** |
| G8 | 배지·검토함·재렌더 | ⬜ | | 대시보드 변경 → 미리보기 첨부 |
| G9 | workflow 증거·관제 | ⬜ | | |
| G10 | 백필 dry-run | ⬜ | | 이것이 곧 "과거 30일 가상 재생" |
| EXP | 30일 가상 재생 + 분석 | ⬜ | | 설계 확정 → `docs/superpowers/specs/2026-08-15-guideline-replay-experiment-design.md` |

## 배포 순서 제약 (계획서 §13 — 어기면 안 된다)

1. G0~G3 먼저 병합. **런타임 자동선정은 현행 그대로.**
2. G4·G5 는 **dry-run 으로만** 배포해 오탐률·파서 건강성 관찰.
3. G2 의 제외 패턴·임계값은 **설정에서만** 조정.
4. G6·G8 은 상태 fixture 로 검증 — 기존 카드 유실 0건.
5. G7 에서 매일 소진 배선. 첫 배포는 `autoPublishThreshold` 보수적으로.
6. G9 증거 검증 뒤 G10 백필 dry-run → 승인 후 apply.

**★ G4/G5(수집 확대)와 G7(자동 발행)을 같은 커밋에 넣지 않는다.**

## PeterJ 확정 5건 (2026-08-14 · 되묻지 말 것)

①-B+①-C 자동 인정 범위를 PT 밖(consensus·statement·focused update·recommendations)까지
넓히고 승인 학회 사이트도 별도 수집 · ②-C 주제 무매칭 tier-1 은 `needsReview` 보존 ·
③-C 신판은 새 카드, 구판은 삭제 없이 `superseded` 배지 · ④-D 매일 시도하되 큐가 비면 건너뜀 ·
⑤-A 수동 URL 은 승인 그 자체(도메인 검증 금지).

**확정이 아닌 것**: 가중치·임계값(`autoPublishThreshold` 등)은 설계서 예시에서 온 잠정값이다.
기관 목록·adapter·selector 도 확정 실물이 아니다.
