# 코드리뷰 변증 토론 — src/utils/dates.js

- 일시(KST): 2026-07-07 21:46
- 대상: `src/utils/dates.js`
- 모델: codex=gpt-5.5 · reviewer/judge=Opus (E2E 드라이런 — 명명 서브에이전트 미등록 세션이라 general-purpose 대체)
- 라운드: 2 (R0 독립 → R1 변증, **수렴=yes** / 캡=4)

## 최종 판정 (심각도별)

### 🟡 Minor
- **F1 · `src/utils/dates.js:11` — 실패 모드 불일치 (confirmed)**
  세 함수의 잘못된 입력 처리가 제각각. 실측: `kstDateStr`/`kstDateSlash`는 Invalid Date에
  예외 없이 `'Invalid Date'`/`'Invalid/Date'`를 **조용히 반환**(오염값이 PubMed mindate·파일명·
  세션ID로 전파), `kstStamp`는 `RangeError` **throw**, 비-Date 입력은 `TypeError`. 방어(`isNaN`) 없음.
  · 실패 시나리오: 외부에서 오염된 Date가 주입되면 한쪽은 무증상 오염, 한쪽은 크래시 → 원인 추적 곤란.

### ⚪ Trivial / open
- **F2 · `src/utils/dates.js:11` — 포맷 추출 방식 비대칭 (open)**
  `kstDateStr`는 `toLocaleDateString('en-CA')` **문자열 포맷에 의존**, `kstStamp`는 `formatToParts`로
  **파트 추출**. "ICU 차이로 en-CA 형식이 깨져 PubMed 형식까지 깨진다"는 A1 주장은 full-icu 실측
  **미재현(이론적)** → open. 단 구조 비대칭 자체는 confirmed(견고성 개선 여지).

## 기각된 주장
- **B1 · `src/utils/dates.js:23` — kstStamp 자정 '24' 렌더** — `hourCycle:'h23'` 명시로 KST 자정이
  `'000000'`으로 정상 렌더('24' 미발생). **codex·Opus 모두 실측 후 자기 철회**로 수렴.

## 미해결(open)
- **F2** — 로케일 포맷 의존이 실환경에서 깨지는지: 이론적 위험만 확인, 현재 Node 환경에선 미재현.

## 수정방안 (우선순위) — ⚠️ 승인 게이트: 미적용
1. **실패모드 통일(F1)** — `dates.js:10~24` 세 함수 진입부에 공통 가드
   `if (!(d instanceof Date) || Number.isNaN(d.getTime())) throw new TypeError('kst*: valid Date required')`
   추가 → 조용한 `'Invalid Date'` 전파 제거, fail-fast 통일.
2. **포맷 추출 통일(F2)** — `dates.js:11` `kstDateStr`을 `kstStamp`와 동일하게
   `Intl.DateTimeFormat(...).formatToParts`로 year/month/day를 뽑아 `'YYYY-MM-DD'` 조립 →
   locale 문자열 포맷 의존 제거, 두 경로 대칭화.

## 라운드 로그(요약)
- **R0** codex 1건(A1 line11 로케일) / Opus 2건(B1 line23 자정'24' open, B2 line11 Invalid Date open) — 서로 다른 지점 포착.
- **R1** 상호 반박·수용:
  - B1: codex 기각 + Opus 자기 철회(h23 실측 '00') → 합의 기각.
  - A1: Opus가 실측으로 핵심 주장 기각(en-CA 안정), 단 구조 비대칭은 confirmed → open.
  - B2: 양측 confirmed(실패모드 불일치 실측).
- **심판** R1에서 `new_rebuttals=0` → **수렴 종료**. F1 confirmed(minor), F2 open(trivial), B1 rejected.

> 이 리포트는 codex-debate 스킬의 **엔드투엔드 검증 산출물**입니다. 실제 코드는 승인 게이트에서
> 정지 — 변경하지 않았습니다.
