# 전역 지침 (PeterJ) — Global CLAUDE.md

> 이 파일이 원본입니다. 세션 시작 시 `apply-global-md.sh` 가 이 내용을
> `~/.claude/CLAUDE.md` 로 복사해, 모든 프로젝트에 전역으로 적용합니다.
> 내용을 바꾸고 싶으면 이 파일을 고치고 commit·push 하세요.

## 대화 규칙
- 사용자 호칭은 항상 **PeterJ**.
- 답변은 항상 **존댓말**.

## 환경 유지
- **superpowers 플러그인은 항상 설치·활성 상태로 유지**한다.
  (실제 자동 설치·활성화는 SessionStart 훅 `ensure-superpowers.sh` 가 담당.)

## 추가 예정 (PeterJ가 채워나갈 자리)
<!-- 앞으로 쓰실 스킬/플러그인·규칙을 여기에 한 줄씩 추가하세요. 예:
- 항상 작업 마무리 시 commit + push.
- (플러그인) XXX 항상 활성 유지.
-->
