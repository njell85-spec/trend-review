# Trend Review — 에이전트 메모

## 사용자
- 호칭은 항상 **PeterJ**, **존댓말** 유지.
- 작업은 대부분 **리모트 컨트롤(안드로이드 폰/태블릿)**. 데스크탑 사용은 어려움.
- **PeterJ가 직접 해야 하는 조작**(설정 변경·토큰/키 발급·계정 인증·앱/웹 UI 탐색 등)은
  **아주 상세한 스텝바이스텝**으로 안내한다: 어느 화면에서 **무엇을 누르고, 어디로 스크롤하고,
  어떤 항목·라벨을 고르는지**까지 구체적으로. "설정에서 바꾸세요" 같은 뭉뚱그린 안내 금지
  (모바일/원격 위주라 화면 맥락 없이는 따라가기 어렵다).
  - **안내 전 최신 UI 확인 필수**: 앱·웹의 메뉴·항목 구성은 수시로 바뀌므로, 기억 속 옛
    화면이 아니라 **현재 최신 구성**을 먼저 확인(공식 문서·웹 검색·PeterJ 스크린샷 요청 등)한
    뒤 그걸 근거로 단계를 짠다. **모바일 폼과 데스크탑 폼은 메뉴 위치·명칭이 다르므로** PeterJ가
    지금 쓰는 쪽 폼 기준으로 안내한다.
  - **UI가 불확실하면 추측을 사실처럼 쓰지 말고** 확인된 부분과 추정 부분을 구분해 표기한다.
    (PeterJ 지시 2026-07-07 · 최신 UI 확인 조항 추가)

## 저장소
- `Trend_Review`(대문자) = 예전 데스크탑(VS Code) 원본.
- `trend-review`(소문자, **현재 작업 home**) = 리모트 컨트롤 연동용. 신규 작업은 여기서.

## 작업 습관
- 작업 **마무리 시 commit + push**, 시작 시 `git pull` 로 데스크탑과 동기화(허브=GitHub).

## 세션 리추얼 (모델 불문 — Opus 세션도 동일 품질을 내기 위한 장치)
- **시작**: `docs/HANDOFF.md` 필독 → §1 "먼저 읽을 파일" 순서대로 맥락 복원.
  §3 확정 결정은 되묻지 않는다. §4 불변식(데일리 코어 무영향)을 깨는 변경 금지.
- **마무리**: HANDOFF §5(완료 상태)·§8(진행 중이던 것) 갱신 + commit/push —
  다음 세션은 이 파일 하나로 이어받는다.
- **규모 있는 작업 4규칙**: ① 계획 문서 먼저(`docs/superpowers/plans/`)
  ② 작은 단위 커밋 ③ 병합 전 병렬 서브에이전트 리뷰(실버그 위주, 가능하면 상위 모델)
  ④ 증거(명령·출력) 없는 완료 주장 금지.
- 반복 판단·검증 작업은 가능하면 **Actions 워크플로우로 고정**(예: `video-sample.yml`,
  `ci.yml`) — 모델 재량 개입을 줄여 세션 품질 편차를 흡수한다.

## 품질 게이트 (이 프로젝트 전용)
- 리포트·발송·대시보드 관련 작업 전 **REPORT_SPEC.md 필독** (단일 기준 문서).
- 대시보드·메시지 포맷 변경 시 push 전 **/preview 스킬로 미리보기 승인** 받기.
- 커밋 전 **`npm run spec-lint` 통과** 확인 (CI 앞단에서도 강제됨).

## Codex MCP (클코 안에서 Codex 사용)
- 셋업·토큰 갱신·트러블슈팅 문서: **`docs/codex-mcp-setup.md`**.
- 전역 자동 규칙(모든 repo에 `.mcp.json` 자동 추가)은 `.claude/global-CLAUDE.md` "Codex MCP" 항 참고.
- PeterJ가 "codex 문서 불러와" 하면 `docs/codex-mcp-setup.md`를 연다.

> 세부 목표·운영 방침은 아직 확정 전이라 여기 못 박지 않음. 대화로 조율하며,
> 현재 작업 중인(확정 아님) 스펙은 REPORT_SPEC.md 참고.

<!-- BEGIN global-config: 전달 형식 규칙 (자동 배포 · 직접 수정하지 마세요) -->
## 전달 형식 (전역 규칙 · PeterJ 확정 2026-07-28)

- **PeterJ가 타이핑하거나 복사·붙여넣기할 내용은 반드시 코드블럭으로 낸다.**
  명령어·경로·시크릿 이름·설정 JSON·붙여넣을 문구 등 **글자 그대로 옮겨야 하는 것은 전부**
  ```로 감싼다. 줄글 안에 섞지 않는다 — 복사 버튼이 생기고 공백·따옴표가 안 뭉개진다.
  **한 줄짜리도 코드블럭으로** 낸다. (읽기만 하는 설명은 평어로 써도 된다.)
- **보고·정리를 `.md` 파일로 만들어 "읽어보세요"라고 하지 않는다.**
  전달은 ① **채팅창에 정리해서 직접 제시** 또는 ② **HTML**(아티팩트 · `display:'render'`)로 한다.
  문서 파일 자체를 만드는 것은 계속 한다 — 핸드오프·설계 문서는 **다음 세션과 정본**을 위한
  것이라 필요하다. 다만 **PeterJ에게 그걸 읽으라고 넘기지 않는다**: 요지는 채팅에 적고,
  파일은 "정본은 여기 있습니다" 수준으로만 언급한다.

- **선택지는 각각 별도 코드블럭으로 낸다** (PeterJ 확정 2026-08-06). 번호 줄글이되
  **선택지 하나마다 코드블럭 하나**로 감싼다. 클릭식 선택 UI는 쓰지 않는다.
  PeterJ가 답할 때 **그 블럭을 복사해 붙여넣으면 끝나서** 타이핑이 없다(모바일에서 특히).
  묶음이면 `대분류-소분류` 번호를 써서 여러 건을 한 번에 답할 수 있게 한다.

## 마무리 시 타워에 진행상황 남기기 (PeterJ 확정 2026-08-06)

PeterJ는 매일 아침 **global-config(타워) 세션에서 저장소별 task 목록**을 보고 그날 열 저장소를
고른다. 그 목록의 정본은 타워의 `data/project-status.json`이고, **타워는 비용 때문에 모든
저장소를 매일 다시 읽지 않는다.** 그러니 **이 저장소에서 일한 세션이 자기 몫을 갱신해야** 목록이 산다.

3종·4종 마무리를 할 때 같이 한다:
- `add_repo njell85-spec/global-config` → `data/project-status.json`에서 **자기 프로젝트만** 찾아
  `items`(done/doing/todo)와 `updated`(오늘 KST)를 갱신 → 브랜치 → PR → 머지.
  **다른 프로젝트 항목은 건드리지 않는다.**
- 서식 문자(`**`)를 넣지 말 것 — 빌더가 전부 HTML 이스케이프해서 별표가 화면에 그대로 뜬다.
- 갱신을 못 했으면 **핸드오프에 "타워 미갱신"이라고 남긴다.**
- 이 저장소의 **결정·관찰도 한 줄씩** 타워 raw 장부(`data/ledger/YYYY-MM.md`)에 append하면 좋다.
<!-- END global-config: 전달 형식 규칙 -->

<!-- BEGIN global-config: 대화 아카이브 -->
## 대화 아카이브 (자동 배포 · 직접 수정하지 마세요)

- **세션 시작 시 `add_repo`로 `njell85-spec/global-config`를 붙여라** (대화 아카이브 보관용).
- 붙이지 않으면 **이 세션 대화는 보관되지 않는다.** 이 repo는 public이라 대화 원문을
  여기 남기지 않고, 붙어 있는 global-config 클론의 전용 브랜치로 자동 보관한다.
  (PeterJ는 이에 대해 아무 말도 하지 않는다 — 세션이 알아서 붙인다.)
<!-- END global-config: 대화 아카이브 -->
